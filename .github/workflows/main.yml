on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp-ecoridenack.alwaysdata.net
          username: ecoridenack
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /www/
          # Configuration pour résoudre l'erreur SSL
          protocol: ftp
          # Désactiver complètement SSL pour éviter l'erreur
          security: strict
          # Paramètres de connexion robustes
          timeout: 300000
          retry-count: 5
          retry-delay: 10000
          # Mode passif pour AlwaysData
          passive: true
          # Exclusions étendues
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/bootstrap/scss/**
            **/node_modules/bootstrap/js/**
            **/node_modules/bootstrap-icons/icons/**
            **/.github/**
            **/*.md
            **/.DS_Store
            **/Thumbs.db
            **/package-lock.json
            **/yarn.lock
            **/*.log

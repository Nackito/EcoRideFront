{
  "name": "ecoridefront",
  "version": "1.0.0",
  "description": "Ce site est un site de covoiturage",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=3000",
    "build": "sass scss/main.scss styles/main.css --style compressed",
    "watch": "sass scss/main.scss styles/main.css --watch",
    "serve": "http-server -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.7",
    "bootstrap-icons": "^1.13.1"
  }
}
